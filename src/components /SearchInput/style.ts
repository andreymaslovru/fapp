import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    justifyContent: 'center',
    alignItems: 'center',
    flexDirection: 'row',
  },
  textInputWrapper: {
    width: 300,
    height: 30,
    borderRadius: 15,
    backgroundColor: '#fff',
  },
});
